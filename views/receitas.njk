{% extends "layout.njk" %}

{% block content %}
    
    <section class="cards">
    {% for item in itens%}
            <div class="card" id="{{item.id}}"><a href="/receita_info?id={{item.id}}">
             <div class="card_image-container" >
                    <img src="{{item.img}}" alt="{{item.title}}">
                </div></a>
                <div class="card_content">
                    <p>{{item.title}}</p>
                </div>
                <div class="card_info">
                    <p>{{item.author}}</p>
                </div>
            </div>            
 {%endfor%}
            </section>
{%endblock%}